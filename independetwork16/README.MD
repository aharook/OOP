# Самостійна робота No16
## Тема: Схема розподілу відповідальностей модуля.
## Мета: Навчитися застосовувати принцип єдиної відповідальності (SRP) для декомпозиції складного модуля на менші, більш сфокусовані класи, а також візуалізувати розподіл відповідальностей за допомогою діаграми класів (UML).


### 1. Створити новий консольний проєкт.
Виконано

### 2. Реалізувати “поганий” клас OrderProcessor згідно з варіантом.
Було реалізовано поганий клас inventoryManager, що порушує SRP, виконуючи кілька відповідальностей. 
Він був Заповнений  методами, які імітують різні дії роботи класу.

### 3. Виконати рефакторинг OrderProcessor для дотримання SRP.
Було виконано рефакторинг inventoryManager та розбито функціональність на окремі класи/інтерфейси, кожен з
яких має єдину відповідальність. Також було Використано принцип інверсії залежностей (DIP), щоб InventoryService залежав від
абстракцій (інтерфейсів), а не від конкретних реалізацій.
• Реалізуйте прості “заглушки” (mock implementations) для цих інтерфейсів.

### 4. Створити діаграму класів (UML) для рефакторингової структури.
Було створено UNL діаграму класів за допомогою plantuml та додано до папки поточного проєкту

### 5. Продемонструвати роботу в методі Main:
Вивід з консолі:

![alt text](image.png)